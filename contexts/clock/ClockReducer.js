
const initialStates = {
    
};


const ClockReducer = (state = {}, { type, action }) => {
    let newState = Object.assign({}, state);
    switch (type) {
        case '':
            break;
        default:
            return newState;
    }
}